//write a c program to read the integer as input from the user and print the odd numbers from 1 to 100
#include<stdio.h>
int main()
{
    int num;
    printf("enter an integer :");
    scanf("%d",&num);
    printf("odd numbers from 1to 100",num);
    for(int i=1;i<=num;i++)
  {
    if(i%2!=0)  
       printf("%d\n",i);
   }
   }
